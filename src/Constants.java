import java.awt.*;

public class Constants {
    final static int WIDTH_PX = Double.valueOf(Toolkit.getDefaultToolkit().getScreenSize().getWidth()).intValue();
    final static int HEIGHT_PX = Double.valueOf(Toolkit.getDefaultToolkit().getScreenSize().getHeight()).intValue();
    final static int ENTITY_SIZE_PX = 4;
}
